/* type line = {
  startPos: (float, float),
  startPos: (float, float),
  vec: (float, float),
}

let getLine = (startPos, endPos) => {
  let (startX, startY) = startPos;
  let (endX, endY) = endPos;
  {
  startPos,
  startPos,
  vec: (endX -. startX, endY -. startY),
  }
}

let getParabola */